import Header from "./Header";

const Navbar = ({headerStyle}) => {
  return (
    <>
        <Header
          headerStyle={
            headerStyle
          }
        />
    </>
  );
};

export default Navbar;
